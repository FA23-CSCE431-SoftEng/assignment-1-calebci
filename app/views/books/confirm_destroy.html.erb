<p style="color: green"><%= notice %></p>

<h1>Are you sure you want to delete this book?</h1>

<p>Title: <%= @book.title %></p>
<p>Author: <%= @book.author %></p>

<%= button_to 'Yes, Delete', @book, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>

<%= button_to 'No, Go Back', book_path(@book), class: 'btn btn-primary' %>